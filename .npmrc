# React Native / Expo autolinking (Gradle + CocoaPods) needs a FLAT node_modules.
# pnpm default is isolated/symlinked, which breaks native autolinking on EAS Build.
# Expo documents node-linker=hoisted as required for pnpm monorepos.
node-linker=hoisted
